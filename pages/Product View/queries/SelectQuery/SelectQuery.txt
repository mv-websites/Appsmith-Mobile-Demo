SELECT * FROM WrARJWM7_actionscheduler_actions
WHERE hook like '%{{data_table.searchText || ""}}%'
ORDER BY {{data_table.sortOrder.column || 'action_id'}} {{data_table.sortOrder.order || "ASC"}}
LIMIT {{data_table.pageSize}}
OFFSET {{(data_table.pageNo - 1) * data_table.pageSize}};